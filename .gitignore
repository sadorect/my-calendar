node_modules
dist
.DS_Store
*.log
.vite
playwright-report
playwright-report/index.html
test-results
# Sync server deployment secrets
sync-server/.env

# Local build-time settings (VITE_SYNC_URL and friends) and the Vercel link
.env.local
.env.*.local
.vercel

# Android (TWA) — the signing key, the build output, and the Gradle project
# Bubblewrap regenerates from android/twa-manifest.json
android/*.keystore
android/dist/
android/app/
android/gradle/
android/gradlew
android/gradlew.bat
android/build.gradle
android/settings.gradle
android/gradle.properties
android/local.properties
android/store_icon.png
android/*.apk
android/*.aab
android/*.idsig
